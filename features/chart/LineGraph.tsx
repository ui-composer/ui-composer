import React, { memo } from 'react';

import { Text, TextProps } from '@ui-composer/react';

export const LineGraph = memo((props: TextProps) => {
  return <Text {...props}>Replace with line graph component</Text>;
});

LineGraph.displayName = 'LineGraph';
