{
  "name": "@ui-composer/mobile-app",
  "version": "0.0.0",
  "private": true,
  "main": "src/main.js",
  "scripts": {
    "clean": "yarn clean:workspace",
    "android": "concurrently 'yarn workspace ui-composer watch' '$(yarn bin expo) start --android'",
    "ios": "concurrently 'yarn workspace ui-composer watch' '$(yarn bin expo) start --ios'",
    "web": "concurrently 'yarn workspace ui-composer watch' '$(yarn bin expo) start --web'",
    "lint": "yarn lint:workspace"
  },
  "dependencies": {
    "@react-navigation/native": "^6.0.10",
    "expo": "^45.0.0",
    "expo-font": "~10.1.0",
    "expo-modules-core": "~0.9.2",
    "expo-splash-screen": "~0.15.1",
    "expo-status-bar": "~1.3.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-native": "^0.68.2",
    "react-native-reanimated": "~2.8.0",
    "react-native-safe-area-context": "4.2.4",
    "react-native-screens": "~3.11.1",
    "ui-composer": "workspace:*"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9",
    "@ui-composer/eslint-config": "workspace:*"
  }
}
